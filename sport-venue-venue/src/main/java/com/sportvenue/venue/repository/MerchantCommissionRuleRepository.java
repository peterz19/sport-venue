package com.sportvenue.venue.repository;

import com.sportvenue.venue.entity.MerchantCommissionRule;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MerchantCommissionRuleRepository extends JpaRepository<MerchantCommissionRule, Long> {
}
