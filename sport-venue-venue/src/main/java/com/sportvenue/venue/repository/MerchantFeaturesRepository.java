package com.sportvenue.venue.repository;

import com.sportvenue.venue.entity.MerchantFeatures;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MerchantFeaturesRepository extends JpaRepository<MerchantFeatures, Long> {
}
