spring:
  application:
    name: venue-service
  config:
    activate:
      on-profile: local
  cloud:
    config:
      uri: http://localhost:8888
      name: venue-service
      profile: local
      label: main 