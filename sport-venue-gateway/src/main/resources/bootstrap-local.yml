spring:
  application:
    name: gateway-service
  config:
    activate:
      on-profile: local
  cloud:
    config:
      uri: http://localhost:8888
      name: gateway
      profile: local
      label: main 