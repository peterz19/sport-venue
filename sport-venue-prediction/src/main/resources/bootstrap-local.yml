spring:
  application:
    name: prediction-service
  config:
    activate:
      on-profile: local
  cloud:
    config:
      uri: http://localhost:8888
      name: prediction-service
      profile: local
      label: main 