spring:
  application:
    name: social-service
  config:
    activate:
      on-profile: local
  cloud:
    config:
      uri: http://localhost:8888
      name: social-service
      profile: local
      label: main 